# 语绘 (Voice Canvas) — 计划 vs 实际

> 2026-06-14 · 最终截止日 · 项目复盘

---

## 一、总体对比

| 维度 | 计划 | 实际 | 判定 |
|------|------|------|------|
| 开发周期 | 6/12 ~ 6/14 | 6/12 ~ 6/14 | ✅ 按时 |
| PR 总数 | 13 | 12（#12 待做，#13 进行中） | ⚠️ 接近完成 |
| 前后端联动 | 全链路串通 | 已串通，可跑通 | ✅ |
| 语音识别 | 七牛云 ASR (主) / Web Speech (备) | 阿里云百炼 Paraformer (主) / Web Speech (备) | 🔄 更换 |
| LLM | DeepSeek V3 via 七牛云 MaaS | DeepSeek V4-Flash 官方直连 | 🔄 更换 |
| 渲染引擎 | 逐笔动画 + pencil/paint 画笔 + 渐变 | 完整实现 | ✅ |
| 对象管理 | ObjectStore + HistoryManager + FuzzyMatcher | 完整实现 | ✅ |
| 对象编辑闭环 | update/move/delete + 模糊匹配修正 | 完整实现 | ✅ |
| UI 打磨 | 状态指示器动画 + 整体样式 | 未开始 | ❌ 待 PR #12 |
| Demo 视频 | bilibili 上传 | 未录制 | ❌ 待 PR #13 |

---

## 二、技术决策变更

### 变更 1: ASR 语音识别 — 七牛云 → 阿里云百炼

| | 计划 | 实际 |
|------|------|------|
| 服务商 | 七牛云短语音听写 | 阿里云百炼 |
| 模型 | — | `paraformer-realtime-v2` |
| 认证 | HMAC-SHA1 签名 (AppID + SecretKey) | Bearer Token (单一 API Key) |
| 实现 | `backend/asr/qiniu_asr.py` | `backend/asr/paraformer_asr.py` |

**原因**: 七牛云 ASR 接口复杂（双 Key + HMAC 签名），阿里云百炼 REST API 更简洁（Bearer Token 直传）。`paraformer-realtime-v2` 支持普通话 + 粤语，同步模式直接返回结果，无需轮询。

**潜在优化**: 后续可评估 Qwen3-Audio-ASR 或 Fun-ASR 等百炼平台更新模型（本次因联网搜索不可用未对比，先保留 `paraformer-realtime-v2`）。

---

### 变更 2: LLM 大模型 — 七牛云 MaaS → DeepSeek 官方直连

| | 计划 | 实际 |
|------|------|------|
| 渠道 | 七牛云 MaaS 代理 (`api.qnaigc.com`) | DeepSeek 官方 API (`api.deepseek.com`) |
| 模型 | `deepseek-v3` | `deepseek-v4-flash` |
| API Key | 七牛云下发 | DeepSeek 平台直接获取 |

**原因**: 
1. **减少中间层** — 七牛云 MaaS 代理增加了一个依赖方，直连更稳定
2. **模型升级** — DeepSeek 官方公告 `deepseek-v3` / `deepseek-chat` 将于 2026/07/24 停用，提前迁移到 V4 系列
3. **性价比** — `deepseek-v4-flash` 速度快、成本低，绘图指令生成场景够用；需要更强推理时可切换 `deepseek-v4-pro`

---

### 变更 3: 默认模型降级策略

| | 计划 | 实际 |
|------|------|------|
| LLM 默认 | `deepseek-v3` | `deepseek-v4-flash` |
| ASR 降级 | ASR 失败 → Web Speech | 同（未变） |
| ASR 不配 | 不可用 | 允许（前端自动降级浏览器语音识别） |

**新增能力**: `.env` 不配 `DASHSCOPE_API_KEY` 也能跑 —— 前端检测 ASR 错误后自动切换浏览器内置 Web Speech API。最少只需一个 DeepSeek Key 即可跑通全链路。

---

## 三、PR 完成情况

### 已完成 ✅

| PR | 内容 | 负责人 | 状态 |
|----|------|--------|------|
| #1-4 | 设计文档 v1→v3 | 芝士番薯 | ✅ 6/12 |
| #5 | TypeScript 类型 + 脚手架 | 月栖白 | ✅ 6/13 |
| #6 | 脚手架整合 (FastAPI + Vite) | 月栖白 | ✅ 6/13 |
| #7 | Canvas 渲染引擎（画笔/渐变/动画） | 芝士番薯 | ✅ 6/13 |
| #8 | 对象管理（ObjectStore/History/Fuzzy） | 月栖白 | ✅ 6/13 |
| #9 | 后端核心（ASR/路由/LLM/修复/校验） | 协作 | ✅ 6/13 |
| #10 | 前端串联（VAD/API/Executor/App/TTS） | 月栖白 | ✅ 6/13 |
| #11 | 对象编辑 + 修正闭环 | 月栖白 | ✅ 6/13 |
| — | API 迁移（DeepSeek 直连 + 阿里云 ASR） | 月栖白 | ✅ 6/14 |
| — | README + GitHub 社区文件 | 月栖白 | ✅ 6/14 |

### 未完成 ❌

| PR | 内容 | 负责人 | 原因 |
|----|------|--------|------|
| #12 | UI 打磨（状态动画、样式） | 芝士番薯 | 时间不足，先暂缓 |
| #13 | Demo 视频录制 + bilibili | 共同 | 待 PR #12 完成后录制 |

---

## 四、保留未做的设计

以下是统一项目计划中设计但未实际实现的部分：

| 设计 | 状态 | 原因 |
|------|------|------|
| `curve` / `arc` 图形指令 | LLM Prompt 中有，前端实现待验证 | 低频使用，未优先 |
| `speak` TTS 指令 | 前端 `useSpeechFeedback.ts` 已预留 | 已实现 |
| 粤语支持 | ASR 参数中配置 `"language_hints": ["zh", "yue"]` | 已配置，未深度测试 |
| VAD 静音检测 | `useVoice.ts` + AnalyserNode | 已实现 |
| 推按录音交互 | VoiceBar 推按 + 状态指示 | 已实现 |
| 四级模糊匹配 | label → 颜色 → 位置 → 最近对象 | 已实现 |
| 修复管道（去md/截断/字段） | `json_repair.py` | 已实现 |
| Command Parser 7 项校验 | `command_parser.py` | 已实现 |

---

## 五、工时估算偏差

| 阶段 | 计划 | 实际 | 偏差 |
|------|------|------|------|
| 设计文档 (PR #1-4) | 1 天 | 1 天 | 无 |
| 脚手架 + 类型 (PR #5-6) | 半天 | 半天 | 无 |
| 渲染引擎 (PR #7) | 半天 | 半天 | 无 |
| 对象管理 (PR #8) | 半天 | 半天 | 无 |
| 后端核心 (PR #9) | 半天 | 半天 | 无 |
| 前端串联 (PR #10) | 半天 | 半天 | 无 |
| 对象编辑 (PR #11) | 半天 | 半天 | 无 |
| API 迁移 | 未计划 | 1 小时 | +1h |
| UI 打磨 (PR #12) | 半天 | 未做 | 待定 |
| Demo + 文档 (PR #13) | 半天 | 进行中 | — |

---

## 六、经验总结

### 做得好的

1. **PR 拆分粒度合理** — 每个 PR 独立可验收，依赖链清晰，并行度高
2. **前后端分离彻底** — 通过 JSON 指令协议解耦，两边独立调试
3. **降级策略到位** — ASR 失败 → Web Speech API，LLM 失败 → 友好提示，单点故障不崩
4. **安全校验层** — Command Parser 7 项校验在 LLM 输出后兜底，哪怕 Prompt 被绕过也不会执行危险指令

### 可改进的

1. **入口文件遗漏** — PR #10 合入后 `main.tsx` 没更新导入 App，导致白屏。应在 PR checklist 中加一项"入口文件同步检查"
2. **API 选型可前置** — 七牛云 ASR 和 MaaS 在实际接入时才发现不够好用，提前做 PoC 验证可避免返工
3. **UI 打磨时间预留不足** — 2.5 天冲刺中 UI 被挤到最后，Demo 效果受影响

---

## 七、提交作品检查清单

- [ ] 代码可正常编译运行
- [ ] README.md 完整
- [ ] 设计文档"计划 vs 实际"完成
- [ ] 无 API 密钥泄露（`.env` 在 `.gitignore` 中）
- [ ] Demo 视频录制 + 上传
- [ ] 提交作品
